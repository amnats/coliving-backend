function profilePostHandler(req, res) {

    // test if authed

    // get request json with fields and photos

    // save photo ids or links. If photos are files then save to db. Or to facebook?
    
    // return either 200 or 500 if something went wrong


    res.send('Got a POST request');

    res.send('Got a POST request');
}

module.exports = {
    profilePostHandler
}